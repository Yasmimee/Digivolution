// someReducer.js
const initialState = null;

const someReducer = (state = initialState, action) => {
  // handle actions and update state accordingly
  return state; // or return a new state based on the action
};

export default someReducer;

  